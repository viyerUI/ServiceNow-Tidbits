## Code to add HTML Tootlip to Catalog Variable 

$j('[data-toggle="tooltip"]').each(function( index ) {
  $j(this).removeData('bs.tooltip');
  $j(this).tooltip({html : true, placement: 'top', });
})
